{{ form_errors(form) }}

{{ form_row(form.title) }}
{{ form_row(form.description) }}

<div class="grid gap-5 sm:grid-cols-2">
    {{ form_row(form.price) }}
    {{ form_row(form.alternativePayment) }}
</div>

{{ form_row(form.category) }}
{{ form_row(form.tags) }}
{{ form_row(form.imageFiles) }}
